the model we are going to use it quite simple for this project.

first i thought the model will use non-learning method. here was the model
Model = KNN with cosine similarity

but then i realised i won't be able to do much comparison in it, becuase thier are few non learning methods.

>so, I discovered some ML models for learning approach. but then it should be a content based recommendation system, not collaborative based becauseof it's higher complexity.

thier are varoius codes i have generated using claude code, thier were 4 models for collaborative-based recommendation system
<h2>Auto-enocder<h2>